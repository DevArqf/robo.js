---
title: "CommandConfig"
description: "Configuration options for Discord commands"
---

## Properties

<TypeTable
  type={{
    __auto: {
      description: 'When true, indicates the configuration was auto-generated',
      type: 'true',
      required: false
    },
    __module: {
      description: 'Name of the module that defined this command',
      type: 'string',
      required: false
    },
    __path: {
      description: 'File system path to the command file',
      type: 'string',
      required: false
    },
    __plugin: {
      description: 'Plugin information object',
      type: 'object',
      required: false,
      properties: {
        name: {
          description: 'Plugin name',
          type: 'string',
          required: true
        },
        path: {
          description: 'Plugin path',
          type: 'string',
          required: true
        }
      }
    },
    contexts: {
      description: 'Array of contexts where this command can be used (e.g., "Guild", "PrivateChannel")',
      type: 'CommandContext[]',
      required: false
    },
    defaultMemberPermissions: {
      description: 'Required permissions for users to use this command',
      type: 'string | number | bigint',
      required: false
    },
    description: {
      description: 'Description text shown in the Discord UI',
      type: 'string',
      required: false
    },
    descriptionLocalizations: {
      description: 'Object mapping language codes to localized descriptions',
      type: 'Record<string, string>',
      required: false
    },
    dmPermission: {
      description: 'When true, the command can be used in DMs',
      type: 'boolean',
      required: false,
      deprecated: true
    },
    integrationTypes: {
      description: 'Array of integration types that can use this command',
      type: 'CommandIntegrationType[]',
      required: false
    },
    nameLocalizations: {
      description: 'Object mapping language codes to localized command names',
      type: 'Record<string, string>',
      required: false
    },
    options: {
      description: 'Array of command options/parameters',
      type: 'readonly CommandOption[]',
      required: false
    },
    sage: {
      description: 'Sage AI-powered command protection configuration, or false to disable',
      type: 'false | SageOptions',
      required: false
    },
    timeout: {
      description: 'Timeout in milliseconds for the command execution',
      type: 'number',
      required: false
    }
  }}
/>

<Callout type="warn" title="Deprecated">
  Use `contexts` instead of `dmPermission` to define where commands can be used.
</Callout>
