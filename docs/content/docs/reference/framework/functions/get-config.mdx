---
title: "getConfig"
description: "Access the currently loaded Robo.js configuration"
---

Returns the currently loaded configuration. This function provides access to all the configuration settings defined in your Robo.js application.

## `getConfig()`

```typescript
function getConfig(): Config | null
```

Returns the currently loaded configuration. May return null if config has yet to load.

<Steps>
  <Step>
    **Returns**
    <ReturnType
      type={{
        type: 'Config | null',
        description: 'The current configuration object, or null if not loaded'
      }}
    />
  </Step>
  ---
</Steps>
