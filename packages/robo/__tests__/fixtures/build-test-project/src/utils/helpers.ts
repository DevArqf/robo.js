/**
 * Helper utilities for testing path alias resolution
 */
export function formatMessage(message: string): string {
	return `[Robo] ${message}`
}

export function add(a: number, b: number): number {
	return a + b
}
