/**
 * Nested command for testing directory traversal
 */
export const config = {
	description: 'A deeply nested command'
}

export default function deep(): string {
	return 'deep command executed'
}
