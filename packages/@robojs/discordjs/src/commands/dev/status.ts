/**
 * Dev status command - Shows bot status information
 */
import { devStatusCommand, devStatusCommandConfig } from '../../core/debug.js'

export const config = devStatusCommandConfig
export default devStatusCommand
