/**
 * Debugging event handler - Handles button interactions for dev command outputs
 * (stack traces, logs buttons, etc.)
 */
import { handleDebugButton } from '../../core/debug.js'

export default handleDebugButton
