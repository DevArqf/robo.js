/**
 * Agent constants
 *
 * Centralized configuration values for the CodeAgent.
 */

/**
 * Maximum number of graph iterations before stopping.
 * Each agent → tools cycle counts as iterations.
 * Increase this if complex tasks are hitting the limit.
 */
export const RECURSION_LIMIT = 67
