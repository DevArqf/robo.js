/**
 * CLI Extension for `robo start` command
 *
 * Identical to dev.ts extension - adds mock mode options.
 * Re-exports from dev.ts for identical behavior.
 */
export { config, before } from './dev.js'
